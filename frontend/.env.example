# Vite development uses its /api proxy. Production can also use /api when
# FastAPI serves frontend/dist from the same process.
VITE_API_URL=/api

# Use the same Google OAuth web client ID as backend/GOOGLE_CLIENT_ID.
VITE_GOOGLE_CLIENT_ID=
